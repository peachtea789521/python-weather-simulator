# Python Weather Simulator 🌦️

A simple Python project that simulates weather data for a given city.  
It generates random temperature, humidity, and condition values.

## 🚀 Features
- Pure Python 3
- Randomized weather data
- Configurable parameters via `config.yml` and `config.json`
- CLI output

## 📦 Run
```bash
python main.py
